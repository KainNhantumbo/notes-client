import { _tags as Container } from '@/styles/modules/_tags';

export default function TagsContainer() {
  
  return (
    <Container>
      <section className='tags-list-container'></section>
      <section className='tags-input-container'></section>
    </Container>
  );
}
